Place model weights here (not tracked by git). To obtain checkpoint files, follow the access instructions in the repository [README](../README.md).

Default ViPDE checkpoint:

- `vipde_vitb_damage_v1.pth` — ViPDE ViT-B, building damage segmentation
