# Scripts

Pipeline and agent CLI scripts (`align_pre_post.py`, VLM arbitration helpers, HF download helpers, etc.) are **not included** in this repository.

They remain private for now. The web UI and `geoagent` / `perception` packages are the supported public surface; full offline pipelines require a private checkout that includes `scripts/`.
